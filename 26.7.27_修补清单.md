# Vector OS Nano 狗臂完整链路优化与修补清单

> 日期：2026-07-27  
> 适用仓库：`/media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano`  
> 目标平台：ROS 2 Humble + Conda + NVIDIA GPU + MuJoCo Go2/Piper 仿真  
> 文档性质：实施计划，不表示下列代码已经完成  
> 建议原则：一个任务一个提交、每完成一阶段再进入下一阶段

---

## 0. 本轮修补要解决什么

当前系统已经不是“完全没有导航/感知/抓取”的空壳，现有链路大致为：

```text
自然语言
  │
  ▼
vector-cli / native loop
  │
  ├── 导航：navigate(x, y)
  │      └── Go2ROS2Proxy
  │            └── FAR 全局可见图规划
  │                  └── localPlanner 局部轨迹
  │                        └── go2_vnav_bridge 跟踪器
  │                              └── set_velocity(vx, vy, wz)
  │                                    └── Go2 Convex MPC
  │                                          └── 关节力矩
  │
  └── 抓取：perception_grasp
         ├── RGB-D
         ├── HSV / GroundingDINO 目标解析
         ├── EdgeTAM / 退化矩形掩码
         ├── 深度反投影和世界坐标变换
         ├── Piper top-down IK
         └── 关节插值、闭合夹爪、抬升
```

现有系统能跑通一部分演示，但还存在以下核心缺陷：

1. native CLI 把完整的命名房间 `NavigateSkill` 排除，给模型暴露的是只接收 `(x, y)` 的合成工具；模型看不到房间中心，因此会猜坐标。
2. `room_layout.yaml` 虽然有正确房间中心和门坐标，但“文件存在”不等于 LLM 或 native 工具能访问它。
3. FAR 和 localPlanner 已经运行，但接口没有明确报告“找到有效全局路径/无路可走”，收到一个新 `/way_point` 就可能被当成 FAR 可用。
4. FAR、TARE 共用 `/way_point`，桥接层无法判断目标来自点到点导航还是探索。
5. RViz 主要看到局部短视野 `/path`，看不到完整全局路径，所以容易误以为系统只做直线导航。
6. 局部跟踪允许倒车，长距离任务会出现狗头背对目标倒着走。
7. 卡住检测会在执行一次 escape 后重置总计时，可能长期循环“卡住—脱困—再次卡住”，却不真正失败或重规划。
8. 地形地图加载后没有正确合并回持久化累积器，保存时可能用较小的新地图覆盖旧地图。
9. “绿色物体”目前优先走 HSV 分割，不一定走 GroundingDINO；EdgeTAM 失败时可能静默退化成矩形掩码，CLI 输出看不出实际用了哪个模型。
10. Piper IK 和关节插值没有完整的自身碰撞/Go2 机身碰撞检查，机械臂可能穿过狗身。
11. Go2 底层当前确实使用 Convex MPC，但控制器模型未必包含 Piper 载荷和组合质心，且上层跟踪速度与转向策略也会放大步态问题。
12. TARE 能做几何前沿探索，但未知环境下的语义房间识别、在线门图构建和 SysNav 风格语义搜索还没有闭环。

最终目标是获得两种清晰、可验证的运行模式：

```text
KNOWN_LAYOUT（已知户型）
  room_layout / SceneGraph 是先验
  命名房间 -> 规范化 room_id -> 门链/全局规划 -> 局部避障 -> 正向行走

UNKNOWN_EXPLORATION（未知环境）
  不预灌房间中心
  TARE 几何探索 + 在线建图 + VLM/SysNav 语义识别
  -> 在线 RoomGraph/DoorGraph -> 找到目标房间 -> 切换点到点导航
```

---

## 1. 不应破坏的约束

以下约束贯穿全部修补：

- **保留** localPlanner 的：

  ```bash
  robot_config:=unitree/unitree_go2
  ```

  这不是多余参数。它让 localPlanner 读取 Go2 对应的尺寸、速度和规划参数。若启动文件名或上游版本确实不接受该参数，应先用 `--show-args` 验证，而不是直接删除。

- `room_layout.yaml` 只是仿真中的语义先验，包括房间中心和门位置；它不是 occupancy grid，也不等价于完整墙体/可通行地图。
- FAR 是全局路径层，localPlanner 是局部避障层，TARE 是探索目标生成层，`go2_vnav_bridge.py` 是局部路径跟踪层，Convex MPC 是腿部动力学控制层。不要把这些层合并成一个模糊的“导航库”。
- 命名房间任务不能让 LLM 自己填写坐标。坐标必须由 SceneGraph 或确定性解析器返回。
- 正式验收时，感知模型失败应显式失败；不能把矩形掩码、前方最近物体或仿真 GT 静默伪装成 GroundingDINO/EdgeTAM 成功。
- 未知环境模式不能读取 `room_layout.yaml` 中的目标房间中心作为导航真值。
- 所有导航失败必须以停车结束，不能在“没有路径”时继续以很小的正速度顶墙。
- 机械臂执行前必须通过整条离散轨迹的碰撞检查，不能只检查终点 IK 是否有解。
- 不修改用户已有的无关改动；每一阶段只提交本阶段文件。

---

## 2. 实施顺序和依赖

| 阶段 | 优先级 | 内容 | 依赖 | 完成后得到什么 |
|---|---:|---|---|---|
| P0 | P0 | 固化基线、日志和回放 | 无 | 能比较修补前后 |
| P1 | P0 | 修复 native CLI 命名房间导航 | P0 | LLM 不再猜房间坐标 |
| P2 | P0 | 已知户型的门链和安全全局路径 | P1 | 跨房间不再直穿墙 |
| P3 | P0 | FAR/TARE 接口解耦和规划状态 | P2 | 能判断路径来源与有效性 |
| P4 | P0 | 跟踪器、正向行走和卡住恢复 | P3 | 不长期倒走、不无限顶墙 |
| P5 | P1 | 地形图持久化修复 | P3 | 多次运行地图不缩水 |
| P6 | P1 | Convex MPC 与 Piper 载荷优化 | P4 | 步态和跟踪更稳定 |
| P7 | P0 | 抓取感知可观测性和严格模式 | P0 | 知道实际用了什么模型 |
| P8 | P0 | 机械臂防穿模和安全轨迹 | P7 | 抓取时不穿 Go2 身体 |
| P9 | P1 | 未知环境语义探索/SysNav 化 | P3、P5 | 边探索边建语义地图 |
| P10 | P0 | 全链路回归和演示验收 | P1–P9 | 可重复的完整 demo |

推荐前四个提交检查点：

```text
checkpoint A: P0
checkpoint B: P1 + P2
checkpoint C: P3 + P4
checkpoint D: P7 + P8
```

P5、P6、P9 可在主链路稳定后继续，不建议一开始同时修改。

---

# P0：基线、日志与可复现性

## P0-01：保存当前工作区状态

### 目的

仓库当前已经有用户修改。修补前先保存差异清单，避免后续无法区分原改动和新改动。

### 操作

在 zsh 终端运行：

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano

mkdir -p artifacts/patch_baseline_20260727
git status --short \
  | tee artifacts/patch_baseline_20260727/git_status.txt
git diff \
  | tee artifacts/patch_baseline_20260727/pre_patch.diff
git rev-parse HEAD \
  | tee artifacts/patch_baseline_20260727/git_head.txt
```

注意：

- 不要执行 `git reset --hard`。
- 不要用 `git checkout -- 文件` 丢弃已有修改。
- `artifacts/` 如果不希望纳入 Git，可放到 `/tmp/vector_patch_baseline_20260727`。

### 验收

- 有当前 commit hash。
- 有修改文件清单。
- 有一份可阅读的 `pre_patch.diff`。

### 回滚点

本任务只生成记录，不修改代码。

---

## P0-02：建立统一运行目录和运行 ID

### 涉及文件

- 新增建议：`vector_os_nano/diagnostics/run_context.py`
- 修改：`scripts/launch_explore.sh`
- 修改：`scripts/go2_vnav_bridge.py`
- 修改：`vector_os_nano/hardware/sim/go2_ros2_proxy.py`
- 修改：`vector_os_nano/skills/perception_grasp.py`

### 实现

每次启动生成：

```text
VECTOR_RUN_ID=20260727_153015_abcdef
VECTOR_RUN_DIR=artifacts/runs/20260727_153015_abcdef
```

运行目录至少保存：

```text
run_meta.json
cli.log
bridge.log
far.log
local_planner.log
tare.log
mpc.log
ros_topics.txt
ros_nodes.txt
scene_graph_before.yaml
scene_graph_after.yaml
terrain_before.pcd
terrain_after.pcd
grasp/
  rgb.png
  depth.npy
  detection.json
  mask.png
  grasp_result.json
```

`run_meta.json` 至少记录：

```json
{
  "run_id": "...",
  "git_head": "...",
  "mode": "known_layout",
  "robot_config": "unitree/unitree_go2",
  "two_way_drive": false,
  "far_config": "config/far_go2_indoor.yaml",
  "tare_config": "config/tare_go2_indoor.yaml",
  "edgetam_path": "...",
  "cuda_visible_devices": "...",
  "scene": "go2_room_piper"
}
```

### 验收

- 每次启动不会覆盖上一次日志。
- 任意失败都能通过一个 `run_id` 找到 CLI、导航、感知和控制日志。

---

## P0-03：采集修补前基线

### 启动

终端 A：

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano
./scripts/launch_explore.sh
```

终端 B：

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano
vector-cli
```

终端 C 采集 ROS 图：

```zsh
source /opt/ros/humble/setup.zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector_navigation_stack/install/setup.zsh

ros2 node list
ros2 topic list
ros2 topic info /goal_point -v
ros2 topic info /way_point -v
ros2 topic info /path -v
ros2 topic hz /way_point
ros2 topic hz /path
```

若工作空间只有 `setup.bash`，zsh 中也可执行：

```zsh
source /opt/ros/humble/setup.bash
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector_navigation_stack/install/setup.bash
```

### 固定基线任务

1. `导航到 dining room`
2. `导航到 kitchen`
3. `从 kitchen 导航到 guest bedroom`
4. `抓取前面的绿色物体`
5. `抓取前面的瓶子`

每个导航任务记录：

- 模型最终调用的工具名和参数。
- 目标坐标。
- 起点/终点。
- 是否经过正确门。
- 是否倒着走。
- 是否触墙。
- FAR 是否打印有效路径。
- `/path` 的 `frame_id`。
- 30 秒内净位移。
- 最终 room_id。

每个抓取任务记录：

- 目标查询字符串。
- 检测器类型。
- 分割器类型。
- mask 像素数。
- 有效深度点数。
- 世界坐标。
- IK 结果。
- 是否穿模。
- 夹爪最终是否持物。

---

# P1：修复 native CLI 的命名房间导航

## P1-01：解释当前问题

当前 `vector_os_nano/vcli/native_loop.py` 中 `_build_motor_tools()` 的逻辑是：

1. 为有移动底座的 world 注入合成 `_NativeBaseNavigateTool`。
2. 遍历正式技能时，明确跳过名称为 `navigate` 的 `NavigateSkill`。
3. 合成工具只接受 `x`、`y`。

因此：

```text
用户：“导航到 dining room”
LLM：必须自己把 dining room 变成坐标
LLM：没有看到 room_layout / SceneGraph 的确定性数据
结果：猜出 navigate(2, 0)
```

而 `config/room_layout.yaml` 中真实中心为：

```yaml
dining_room: [3.0, 7.5]
```

文件存在但未暴露给 native LLM，所以模型猜错并不矛盾。

---

## P1-02：拆分两个明确工具

### 目标接口

不要再让一个模糊的 `navigate` 同时承担房间和坐标。

```text
navigate_room(room: str)
navigate_xy(x: float, y: float)
```

可选后续接口：

```text
search_room(room: str)
terminal_dock(target_x, target_y, heading)
```

### 涉及文件

- `vector_os_nano/vcli/native_loop.py`
- `vector_os_nano/skills/navigate.py`
- `vector_os_nano/vcli/primitives/navigation.py`
- `vector_os_nano/vcli/tools/scene_graph_tool.py`
- `vector_os_nano/vcli/worlds/go2_sim_oracle.py`
- `vector_os_nano/vcli/worlds/arm_sim_oracle.py`

### 实现步骤

1. 将 `_NativeBaseNavigateTool` 重命名为 `_NativeNavigateXYTool`。
2. schema 名称改为 `navigate_xy`，只接收数值 `x`、`y`。
3. 新增 `_NativeNavigateRoomTool`，只接收字符串 `room`。
4. `navigate_room.execute()` 不自己维护坐标表，而是调用正式 `NavigateSkill.execute({"room": room}, context)`，或抽取共享的 `RoomResolver`。
5. 不再跳过完整 `NavigateSkill`，但要避免同名冲突；推荐正式技能对 native 层暴露为 `navigate_room`。
6. 两个工具的结果中统一返回：

   ```json
   {
     "goal_id": "...",
     "goal_type": "room",
     "requested_room": "dining room",
     "canonical_room": "dining_room",
     "target_xy": [3.0, 7.5],
     "source": "scene_graph",
     "planner": "far",
     "arrived": true
   }
   ```

7. 不允许 room 工具在解析失败后要求 LLM 猜坐标。失败应返回：

   ```text
   unknown_room: Available rooms are [...]
   ```

### 推荐共享解析器

新增：

```text
vector_os_nano/navigation/room_resolver.py
```

接口示意：

```python
@dataclass(frozen=True)
class ResolvedRoom:
    requested: str
    canonical: str
    center: tuple[float, float]
    source: str


class RoomResolver:
    def __init__(self, scene_graph):
        self._scene_graph = scene_graph

    def resolve(self, query: str) -> ResolvedRoom:
        # 1. 中文/英文别名规范化
        # 2. 只在 SceneGraph 已知房间中匹配
        # 3. 从 SceneGraph 获取中心
        # 4. 失败则抛出 UnknownRoom，不生成猜测坐标
        ...
```

别名至少支持：

| 输入 | canonical room_id |
|---|---|
| 客厅、living room | `living_room` |
| 餐厅、dining room | `dining_room` |
| 厨房、kitchen | `kitchen` |
| 书房、study | `study` |
| 主卧、master bedroom | `master_bedroom` |
| 客卧、客房、guest bedroom | `guest_bedroom` |
| 卫生间、浴室、bathroom | `bathroom` |
| 走廊、hallway | `hallway` |

### 测试

修改或新增：

- `tests/unit/test_navigate_skill.py`
- `tests/unit/test_navigate_coordinate.py`
- `tests/unit/vcli/test_native_loop.py`
- `tests/vcli/test_native_loop_trichotomy_pty.py`
- 新增 `tests/unit/vcli/test_native_room_navigation.py`

必须覆盖：

```text
“导航到 dining room” -> navigate_room(room="dining room")
                       -> canonical=dining_room
                       -> target=(3.0, 7.5)

“去厨房”              -> navigate_room(room="厨房")
                       -> canonical=kitchen

“去 (2.5, 1.2)”       -> navigate_xy(x=2.5, y=1.2)

“去一个不存在的房间”   -> unknown_room
                       -> 不调用 navigate_xy
```

### 验收标准

- LLM 不再为命名房间产生 `(2, 0)` 之类的猜测。
- `dining room` 必须解析成 `(3.0, 7.5)`。
- 房间名称不存在时，机器人保持停止。
- 坐标导航仍可供桌前 standoff、对位和调试使用。

### 回滚点

只回滚 native 工具注册层，不需要动 FAR/localPlanner。

---

## P1-03：向 native prompt 暴露确定性语义，而不是原始 YAML

### 原则

可以把“当前已知房间名”提供给 LLM，但坐标选择仍由工具内部完成。不要把一大段 YAML 交给 LLM 再让它抄坐标。

### 实现步骤

1. 修改 `_native_system_prompt()`，增加来自 live SceneGraph 的房间词表：

   ```text
   Known rooms: living_room, dining_room, kitchen, ...
   For a named room, call navigate_room(room=...).
   Never invent room coordinates.
   Use navigate_xy only when the user explicitly gives a coordinate
   or an earlier perception/planning tool returns one.
   ```

2. 房间词表从 `spatial_memory.get_all_rooms()` 生成，不直接硬编码。
3. `known_layout` 模式中可显示所有先验房间。
4. `unknown_exploration` 模式中只显示已发现房间。

### 验收

- Prompt 和工具都来自同一个 SceneGraph。
- 未知探索时，LLM 看不到尚未发现的房间。

---

## P1-04：增加房间级验证谓词

### 当前问题

当前会出现：

```text
verify nearest_room('dining room')
```

该表达式的语义和参数形式不稳定，并且可能得到 `NOT_GRADED`。坐标导航又会因为执行动作来自后台 bridge 而显示 `UNCAUSED`。

### 目标

新增：

```text
in_room("dining_room")
```

### 实现

在仿真 oracle 中：

```python
def in_room(room_id: str) -> bool:
    canonical = resolver.canonicalize(room_id)
    current = scene_graph.room_at(robot_x, robot_y)
    return current == canonical
```

不要只用“距房间中心小于某个半径”，因为房间形状可能不是圆。已知仿真应优先使用房间 polygon；暂时没有 polygon 时才用最近中心并记录 `verification_mode=nearest_center`。

### 验收

```text
navigate_room("dining_room")
verify in_room("dining_room") -> PASS
```

不再用 `nearest_room('dining room')` 作为主验收。

---

## P1-05：修复导航动作的 actor causation

### 当前问题

导航速度由 bridge 后台线程持续写入，native actor-causation 统计可能没有把它计为当前工具导致的动作，因此真实移动也可能被标记为 `UNCAUSED`。

### 实现

在 `Go2ROS2Proxy` 和 native loop 间传递 `goal_id`：

```text
native tool begins goal_id=G123
Go2ROS2Proxy publish goal G123
bridge accepts G123
bridge publishes non-zero velocity with G123
proxy returns executed_command_count > 0
native loop records actor=CAUSED
```

建议导航结果新增：

```json
{
  "goal_id": "G123",
  "cmd_vel_count": 417,
  "nonzero_cmd_duration_s": 21.3,
  "distance_travelled_m": 5.8,
  "actor_caused": true
}
```

### 测试

- 后台无目标时的自然漂移不能算 CAUSED。
- 目标 G123 的速度不能记到目标 G124。
- 真实移动但未到达应为 `RAN`，不是 `GROUNDED`。
- 到达且 `in_room()` 通过才是 `GROUNDED`。

---

# P2：已知户型中的安全跨房间导航

## P2-01：明确 `room_layout.yaml` 的职责

当前文件：

```text
config/room_layout.yaml
```

它包含：

- 房间中心。
- 房间间门的位置。

它不包含：

- 墙的占用栅格。
- 门宽。
- 机器人膨胀半径。
- 某条中心连线是否可通行。
- 动态障碍。

因此，命名房间导航不能简单执行：

```text
当前位置 -> 房间中心
```

必须执行：

```text
当前房间
 -> 当前房间通向 hallway 的门前点
 -> 门中心
 -> 门后点
 -> 中间门链
 -> 目标房间安全进入点
 -> 目标房间中心或任务 standoff
```

---

## P2-02：扩展房间布局为可执行门拓扑

### 建议新格式

保持向后兼容，在 `config/room_layout.yaml` 中逐步加入：

```yaml
schema_version: 2

rooms:
  dining_room:
    center: [3.0, 7.5]
    polygon:
      - [0.0, 5.0]
      - [6.5, 5.0]
      - [6.5, 10.0]
      - [0.0, 10.0]
    aliases: [dining room, 餐厅]

doors:
  dining_room-hallway:
    center: [6.5, 8.0]
    width: 1.2
    normal: [1.0, 0.0]
    room_a_standoff: [6.0, 8.0]
    room_b_standoff: [7.0, 8.0]
```

### 实现步骤

1. 为旧格式 `[x, y]` 保留 loader。
2. 新格式读入 SceneGraph 的 RoomNode 和 DoorEdge。
3. 校验每个门连接的两个房间都存在。
4. 校验 standoff 点分别位于门两侧。
5. 根据 Go2 footprint 对门宽做可通行判断。
6. 在 SceneGraph 中保存 `source=layout_prior`。
7. 探索观测到的门保存 `source=observed` 和置信度，避免覆盖高置信先验时无记录。

### 测试

新增：

```text
tests/unit/test_room_layout_v2.py
tests/unit/test_scene_graph_door_topology.py
```

校验：

- 旧 YAML 仍能加载。
- dining_room 到 kitchen 的门链经过 hallway。
- 不连通房间返回 `no_route`。
- 门宽不足时返回 `door_too_narrow`。

---

## P2-03：已知户型导航必须先求门链

### 涉及文件

- `vector_os_nano/core/scene_graph.py`
- `vector_os_nano/skills/navigate.py`
- `vector_os_nano/hardware/sim/go2_ros2_proxy.py`
- `vector_os_nano/vcli/tools/scene_graph_tool.py`

### 实现步骤

1. 用机器人当前位置确定 `src_room`。
2. 用目标 room_id 得到 `dst_room`。
3. 调用 `SceneGraph.get_door_chain(src_room, dst_room)`。
4. 将门链扩展为安全 waypoint：

   ```text
   door_a_pre -> door_a_center -> door_a_post
   ...
   target_room_entry -> target
   ```

5. 每个 waypoint 都要带：

   ```json
   {
     "kind": "door_pre|door_center|door_post|room_goal",
     "room_from": "...",
     "room_to": "...",
     "xy": [x, y],
     "tolerance": 0.5
   }
   ```

6. FAR 优先对整条 waypoint 序列逐段规划。
7. FAR 明确报告某段无路时停止，不能直接跳过门点去终点。
8. 到门中心前降低速度，并禁止倒车。
9. 通过门后再允许 localPlanner 恢复正常速度。

### 为什么门链不是 FAR 的替代

门链解决的是高层拓扑：

```text
应该经过哪几个门
```

FAR/localPlanner 解决的是几何路径：

```text
从当前位姿如何避障到下一个门点
```

两者都需要。

### 验收

- living_room -> dining_room 至少经过 `living_room-hallway` 和 `dining_room-hallway`。
- living_room -> kitchen 不允许房间中心连线直接穿墙。
- 若 FAR 在某一门段返回 `NO_PATH`，机器人停车并返回失败。

---

## P2-04：增加全局路径可视化

### 当前问题

现有 `/path` 多半是 localPlanner 在 vehicle frame 中的短程轨迹。即使它看起来是一条直线，也不能据此判断 FAR 没有运行。

### 新增 topic

```text
/far/global_path          nav_msgs/Path
/local_planner/path       nav_msgs/Path
/nav/executed_path        nav_msgs/Path
/scene_graph/door_path    nav_msgs/Path 或 MarkerArray
/nav/current_goal         自定义消息或 JSON String
```

若短期不修改第三方 FAR，可在适配层重建并转发，但 topic 名必须明确来源。

### RViz 配置

修改：

```text
config/vnav.rviz
```

推荐颜色：

| 数据 | 颜色 |
|---|---|
| 门链 | 紫色 |
| FAR 全局路径 | 蓝色 |
| localPlanner 局部路径 | 绿色 |
| 实际轨迹 | 黄色 |
| 当前目标 | 红色球 |
| 障碍点云 | 红/橙 |

### 验收

- 能同时看到“门级拓扑路径、FAR 全局路径、局部路径、实际轨迹”。
- 不再只依靠终端的一条 `/path` 判断全局规划是否存在。

---

# P3：解耦 FAR、TARE 和导航状态

## P3-01：拆分 `/way_point`

### 当前风险

FAR 和 TARE 的数据流最终共用 `/way_point`。bridge 不知道 waypoint 属于：

- 用户点到点导航；
- FAR 的中间点；
- TARE 的探索点；
- 旧进程遗留消息。

### 目标接口

```text
/nav/goal_room
/nav/goal_point
/far/way_point
/tare/way_point
/nav/way_point
/nav/mode
```

增加 `WaypointMux`：

```text
mode=NAVIGATE -> 只转发 FAR/door-chain waypoint
mode=EXPLORE  -> 只转发 TARE/FAR exploration waypoint
mode=DOCK     -> 不接收远程 waypoint，只接收 terminal controller
mode=IDLE     -> 不转发，发布零速度
```

### 实现步骤

1. 新增节点建议：

   ```text
   vector_os_nano/ros2/nodes/waypoint_mux.py
   ```

2. 每次切换 mode 增加 generation 编号。
3. 丢弃上一 generation 的迟到消息。
4. 每个 goal 使用唯一 `goal_id`。
5. mode 切换时先发布 stop，再清理旧路径。
6. `NavigateSkill` 设置 `mode=NAVIGATE`。
7. `ExploreSkill` 设置 `mode=EXPLORE`。
8. 抓取对位设置 `mode=DOCK`。
9. 完成/失败/取消后设置 `mode=IDLE`。

### 验收

- TARE 在 `Waiting for start signal` 时不会影响普通导航。
- 普通导航不会向无效的 TARE `/reset_waypoint` 发送恢复请求。
- 取消探索后，旧 TARE waypoint 不会让狗继续移动。

---

## P3-02：给 FAR 增加显式规划状态

### 当前问题

“收到一个新鲜 `/way_point`”不等于 FAR 找到了有效的全局路径。

### 建议状态

```text
IDLE
GOAL_ACCEPTED
BUILDING_GRAPH
VALID_PATH
NO_PATH
TRACKING
ARRIVED
CANCELLED
ERROR
```

消息至少包含：

```json
{
  "goal_id": "G123",
  "state": "VALID_PATH",
  "graph_nodes": 35,
  "path_points": 48,
  "path_length_m": 9.4,
  "next_waypoint": [6.0, 3.0],
  "reason": ""
}
```

若不便修改第三方 FAR 源码，先在代理层通过以下信号综合判断：

- 目标提交时间。
- FAR 图更新时间。
- path search 输出时间。
- global path 点数。
- waypoint 是否属于当前 goal generation。
- waypoint 与最终目标/门链的几何一致性。

但长期仍应给 FAR 增加正式状态 topic。

### 验收

- `NO_PATH` 时 0.5 秒内停车。
- 不允许把旧 waypoint 当成新 goal 的 `VALID_PATH`。
- 只有 `VALID_PATH` 才启动路径跟踪。

---

## P3-03：路径 frame 必须显式转换

### 当前风险

localPlanner 的 `/path` 可能位于 `vehicle`、`map`、`odom` 或传感器 frame。跟踪器若默认所有点都在车体坐标，会产生错误转向。

### 实现步骤

在 `scripts/go2_vnav_bridge.py`：

1. 读取 `Path.header.frame_id`。
2. 若是 `vehicle`/`base_link`，直接按车体坐标跟踪。
3. 若是 `map`/`odom`，通过 TF 转换到当前 `base_link`。
4. 若为空或未知：

   - 严格模式：拒绝路径并停车；
   - 调试模式：记录警告，但不能静默假设。

5. 日志打印：

   ```text
   path_frame=vehicle points=101 first=(...) last=(...)
   transformed_to=base_link transform_age_ms=...
   ```

### 测试

新增：

```text
tests/unit/test_path_frame_transform.py
```

覆盖：

- vehicle frame。
- map frame + 90° 朝向。
- 过期 TF。
- 空 frame。

---

# P4：正向行走、路径跟踪和卡住恢复

## P4-01：长程导航默认禁止倒车

### 根因

两个层面都允许倒车：

1. `scripts/launch_explore.sh` 当前包含：

   ```bash
   twoWayDrive:=true
   ```

2. `scripts/go2_vnav_bridge.py` 在目标位于身后时会发送负 `vx`，墙边 escape 也可能反向。

### 修改

普通长程导航：

```bash
ros2 launch local_planner local_planner.launch \
  robot_config:=unitree/unitree_go2 \
  autonomyMode:=true \
  joyToSpeedDelay:=0.0 \
  twoWayDrive:=false
```

再次强调：只把 `twoWayDrive` 改为 `false`，**保留** `robot_config:=unitree/unitree_go2`。

bridge 状态机改成：

```text
ALIGN:
  |heading_error| > 35° -> vx=0, wz=限幅角速度

TRACK:
  |heading_error| <= 35° -> vx>0, wz=路径曲率控制

DOOR:
  低速、正向、限制横移

DOCK:
  允许小范围前后微调

ESCAPE:
  仅在碰撞恢复时允许短时倒车
```

### 推荐参数起点

```yaml
navigation:
  long_range:
    allow_reverse: false
    align_enter_deg: 35
    align_exit_deg: 15
    max_vx: 0.30
    max_vy: 0.12
    max_wz: 0.55
  door:
    max_vx: 0.16
    max_vy: 0.05
    max_wz: 0.35
  docking:
    allow_reverse: true
    max_vx: 0.08
    max_vy: 0.06
    max_wz: 0.25
```

先使用保守值，稳定后再调快。

### 验收

- 目标在身后时先原地转向，不连续倒走。
- 长程路径中负 `vx` 总时长接近 0。
- 只有 DOCK/ESCAPE 模式可出现有界负 `vx`。

---

## P4-02：重写进度监视器，修复无限 escape

### 当前缺陷

bridge 中 `_stuck_wall_clock` 在一次 escape 后被重置，因此可能不断：

```text
Stuck 4s -> Stuck 8s -> escape -> reset -> Stuck 4s ...
```

总超时很难真正触发。

### 目标结构

把“本次卡住时间”和“整个 goal 累计无进展”分开：

```python
@dataclass
class ProgressState:
    goal_id: str
    goal_started_at: float
    last_progress_at: float
    best_goal_distance: float
    best_path_arc_remaining: float
    escape_count: int
    cumulative_stuck_s: float
```

进展不能只看终点欧氏距离，还应看：

- 沿局部/全局路径的剩余弧长。
- 机器人真实位移。
- 当前门链 waypoint 的距离。
- heading 是否在改善。

### 建议策略

```text
3 s 无进展：减速并重新 ALIGN
6 s 无进展：请求 localPlanner 重规划
10 s 无进展：请求 FAR 对当前门段重规划
第 1 次恢复：短时后退 0.15–0.25 m + 转向
第 2 次恢复：换另一侧旋转/侧移
累计 15 s 无进展或 2 次恢复失败：停车并返回 STALLED
```

escape 之后：

- 可以重置“连续卡住计时”；
- **不能**重置 `cumulative_stuck_s` 和 `escape_count`；
- 只有净进展超过阈值才更新 `last_progress_at`。

### 禁止行为

没有有效路径时不要发送：

```python
vx = 0.05
```

应发送全零速度并请求重规划。

### 测试

扩展：

- `tests/harness/test_level27_stuck_escape.py`
- `tests/harness/test_level31_wall_escape.py`
- 新增 `tests/unit/test_progress_monitor.py`

### 验收

- 面对封闭墙体时 15 秒左右明确失败并停车。
- 不出现无限 escape 循环。
- 沿墙绕行时不会仅因终点距离短暂不变而误报。

---

## P4-03：使用纯跟踪或 Stanley 型局部路径控制

### 目标

现有跟踪器中的多处分支、特殊门口阈值和逃逸逻辑容易互相干扰。将正常路径跟踪与恢复逻辑分开。

### 实现建议

正常 TRACK 只做：

1. 找路径上离机器人最近点。
2. 根据速度选择 lookahead：

   ```text
   lookahead = clamp(0.35 + 0.8 * speed, 0.35, 0.8)
   ```

3. 计算车体坐标系目标点。
4. heading error 大则进入 ALIGN。
5. 小误差时：

   ```text
   wz = k_heading * heading_error + k_curvature * curvature
   vx = max_vx * heading_scale * obstacle_scale
   vy 只用于小量侧向误差，不作为主要跨房导航手段
   ```

6. 进入门区时切换 DOOR 参数集。
7. 避障/无路/卡住交给独立 supervisor，而不是塞进同一控制公式。

### 测试

- `tests/harness/test_level24_path_follower.py`
- 新增 S 形路径、90° 转弯、门口、目标在身后测试。

### 验收

- 路径横向误差 P95 小于 0.25 m。
- 90° 转弯不倒车。
- 门口不出现大幅横摆。

---

## P4-04：导航与抓取对位分层

### 当前问题

长程导航只保证进入目标区域，不保证相机正对物体；抓取又需要非常具体的 base 位姿和朝向。

### 新链路

```text
navigate_room / navigate_xy
  -> 到达粗目标区域
  -> stop FAR/TARE
  -> perception_scan
  -> compute_grasp_standoff
  -> terminal_dock
  -> verify dock pose
  -> perceive again
  -> grasp
```

### 实现

1. FAR 负责把 Go2 带到目标附近 0.5–1.0 m。
2. 完成后 mode 切换到 `IDLE`，清空路径。
3. 感知扫描确定目标世界位置。
4. 根据机械臂工作空间计算 standoff 和期望 yaw：

   ```text
   base_xy = object_xy - clearance * [cos(yaw), sin(yaw)]
   yaw = atan2(object_y - base_y, object_x - base_x)
   ```

5. terminal docking 只在小范围运行，允许少量倒车/横移。
6. 对位收敛标准示例：

   ```text
   position error < 0.08 m
   heading error < 10°
   object in camera central ROI
   pre-grasp IK has solution
   ```

7. 对位后重新拍摄，不能完全依赖远处观察得到的旧深度。

---

# P5：地形地图持久化

## P5-01：修复“加载 8955 点、保存只剩 4184 点”

### 根因

当前大致存在两个集合：

- 从旧文件加载用于 replay 的点。
- 本轮 `TerrainAccumulator` 新积累的点。

旧点没有合并进入 accumulator，保存时可能仅保存本轮局部点，从而覆盖更完整的旧地图。

### 涉及文件

- `scripts/go2_vnav_bridge.py`
- `tests/harness/test_level30_terrain_persist.py`
- `tests/harness/test_level36_terrain_replay.py`

### 实现步骤

1. `TerrainAccumulator` 增加：

   ```python
   def merge_points(self, points, source: str) -> MergeStats:
       ...
   ```

2. 启动时加载旧地图后立即 `merge_points(loaded, source="disk")`。
3. replay 和 persist 使用同一个 accumulator 快照。
4. 保存前统计：

   ```text
   loaded_voxels
   current_voxels
   merged_voxels
   covered_area_m2
   bounds
   ```

5. 加覆盖保护：

   - 新文件 voxel 数显著减少时不直接覆盖；
   - 地图 bounds 缩小过多时告警；
   - 除非显式 `fresh_map=true`，否则不允许清空旧地图。

6. 使用原子保存：

   ```text
   terrain_map.pcd.tmp -> fsync -> rename terrain_map.pcd
   ```

7. 保留最近 3 份版本：

   ```text
   terrain_map.pcd
   terrain_map.pcd.prev1
   terrain_map.pcd.prev2
   ```

### 验收

- 没有显式 fresh map 时，二次启动/退出后地图覆盖范围不缩水。
- 异常中断不会留下半写文件。
- 测试中 old + new 点的并集可恢复。

---

## P5-02：不要只为每个 XY 保存最大 Z

### 当前问题

仅保存 XY voxel 的最大 Z 会丢失：

- 地面和障碍的区别。
- 垂直结构。
- 桌面下方/门洞等多层信息。

### 改进

至少保存：

```text
x, y, z
class = ground | obstacle | ceiling | unknown
observation_count
last_seen
confidence
```

可采用：

- 3D voxel hash；或
- 2.5D elevation map，分别保存 ground_z、max_obstacle_z、variance。

室内 Go2 导航更推荐先实现 2.5D：

```python
Cell:
  ground_mean_z
  ground_var
  obstacle_min_z
  obstacle_max_z
  obstacle_count
  traversability
```

### 验收

- 地面不会因为同 XY 的桌面点而变成整格高障碍。
- 门洞可通行区域不会被门框高点完全封死。

---

## P5-03：区分导航和探索对地图 replay 的影响

### 当前问题

普通 `/tmp/vector_nav_active` 可能被当成“新探索开始”，导致启动地图 replay 被取消。

### 修改

不要再依赖单个临时文件表达多种状态。至少拆成：

```text
/tmp/vector_nav_mode.json
{
  "mode": "IDLE|NAVIGATE|EXPLORE|DOCK",
  "goal_id": "...",
  "generation": 3
}
```

更推荐使用 ROS topic/service，并把临时文件只作为崩溃恢复状态。

规则：

- `NAVIGATE`：继续 replay 已知地图。
- `EXPLORE`：继续使用旧地图，同时累积新观测。
- `fresh_exploration=true`：用户明确要求时才清空。
- `DOCK`：冻结全局导航，但不删除地图。

---

# P6：Convex MPC 和带机械臂 Go2 的运动优化

## P6-01：确认当前底层控制链

当前仿真日志出现 QP/MPC 初始化，且项目使用：

```text
/media/fishyu/fish-14tb-2/YuXi/go2armagent/third_party/go2-convex-mpc
```

因此当前腿部运动底层是 Convex MPC，而不是 FAR/localPlanner 直接控制关节。

完整职责：

```text
FAR/localPlanner：决定往哪里走
bridge follower：生成 vx/vy/wz
Convex MPC：把期望机身速度变成足端力/关节力矩
MuJoCo：执行动力学
```

看起来“狗走得不好”，可能来自任一层，不能仅通过画面断言 MPC 算法失败。

---

## P6-02：记录 MPC 输入输出和实际跟踪误差

### 新增日志

每 10–20 Hz 记录：

```text
timestamp
mode
vx_cmd, vy_cmd, wz_cmd
vx_actual, vy_actual, wz_actual
roll, pitch, yaw
base_height
foot_contacts
foot_slip
joint_torque_peak
QP status
QP solve time
arm joint pose
grasped payload state
```

计算：

```text
velocity tracking RMSE
yaw-rate tracking RMSE
roll/pitch P95
foot-slip ratio
QP failure rate
```

### 验收

先能回答“是局部路径命令不合理，还是 MPC 没跟上”，再调参。

---

## P6-03：加入 Go2 + Piper 组合质量参数

### 当前风险

基础 Go2 MPC 模型可能只包含裸机质量和惯量；仿真机身上安装 Piper 后：

- 总质量增加。
- 质心上移/偏移。
- 机身惯量变化。
- 机械臂动作带来时变载荷。

### 实现步骤

1. 从 MuJoCo 模型计算：

   - Go2 + Piper 总质量。
   - 初始 stow 姿态组合质心。
   - 组合惯量。

2. 增加配置：

   ```yaml
   mpc:
     robot_model: go2_piper
     payload_mass: ...
     com_offset: [x, y, z]
     inertia: [...]
   ```

3. 先用固定 stow 姿态的等效参数。
4. 长期可根据 arm joint state 在线更新近似 COM，但更新频率不必与 MPC 相同。
5. 导航时强制机械臂进入 `navigation_stow`，减少时变质心。
6. 抓取/搬运时切换更保守步态参数。

### 安全参数起点

- 降低最大线速度。
- 降低最大 yaw rate。
- 增加姿态稳定权重。
- 对夹持物状态进一步降速。

### 验收

- stow 状态下长程导航 roll/pitch 明显减小。
- 持物移动时不频繁 QP 失败。
- 不通过提高腿部力矩上限掩盖上层路径错误。

---

## P6-04：建立 arm/base 模式互锁

定义：

```text
NAVIGATION_STOW
PERCEPTION_STOW
MANIPULATION
CARRY
FAULT
```

约束：

- `NAVIGATION_STOW` 才允许快速长程移动。
- `MANIPULATION` 时 base 默认锁止，除 terminal dock 外速度为零。
- `CARRY` 时降低速度并保持安全持物姿态。
- arm 轨迹正在执行时不能接受 FAR waypoint。
- 导航开始前若机械臂无法收回，导航失败而不是强行走。

---

# P7：感知抓取链透明化和严格模式

## P7-01：明确当前实际路由

当前 `perception_grasp` 并非所有目标都走 GroundingDINO：

```text
“绿色物体”“红色瓶子”
  -> parse_color()
  -> HSV/front_object_mask

“瓶子”“杯子”“banana”
  -> GroundingDINO 检测框
  -> EdgeTAM 分割

“前面的东西”
  -> 基于深度/几何的 front-object
```

因此一次成功的：

```text
perception_grasp -> holding_object('pickable_bottle_green')
```

只说明最终抓取验证通过，不能证明 GroundingDINO 和 EdgeTAM 都运行了。

---

## P7-02：给每次感知输出完整 diagnostics

### 涉及文件

- `vector_os_nano/skills/perception_grasp.py`
- `vector_os_nano/perception/front_object.py`
- `vector_os_nano/perception/grounding_dino.py`
- `vector_os_nano/perception/tracker.py`
- 感知 backend 的 `detect()` / `segment()` 适配层

### 建议结构

```python
@dataclass
class GraspPerceptionDiagnostics:
    query: str
    resolver: str
    detector: str
    detector_model: str | None
    detection_label: str | None
    detection_score: float | None
    bbox: list[float] | None
    segmenter: str
    segmenter_model: str | None
    fallback_used: bool
    fallback_reason: str | None
    mask_pixels: int
    valid_depth_points: int
    camera_xyz: tuple[float, float, float] | None
    world_xyz: tuple[float, float, float] | None
    frame_id: str
```

CLI 输出示例：

```text
Perception:
  resolver=hsv_color
  detector=none
  segmenter=hsv_mask
  mask_pixels=812
  valid_depth_points=647
  target_world=(...)
```

或：

```text
Perception:
  resolver=open_vocabulary
  detector=grounding_dino
  detector_model=grounding-dino-tiny
  score=0.71
  segmenter=edgetam
  fallback_used=false
```

### 验收

通过 CLI 和日志可以确定：

- 是否调用 GroundingDINO。
- 是否调用 EdgeTAM。
- 是否用了退化掩码。
- 最终抓取点来自多少有效深度点。

---

## P7-03：增加正式验收严格模式

### 新增环境变量

```dotenv
VECTOR_GRASP_STRICT_MODE=1
VECTOR_REQUIRE_GROUNDING_DINO=1
VECTOR_REQUIRE_EDGETAM=1
VECTOR_ALLOW_BOX_MASK_FALLBACK=0
VECTOR_ALLOW_SIM_GROUND_TRUTH=0
EDGETAM_MODEL_PATH=/media/fishyu/fish-14tb-2/YuXi/go2armagent/models/EdgeTAM-hf
```

说明：

- `EDGETAM_MODEL_PATH` 直接指定本地模型目录是合理做法，不需要再复制到 `~/.cache`。
- `tracker.py` 当前已经按 `model_name -> EDGETAM_MODEL_PATH -> 默认 HF repo` 选择。
- 验证时应检查实际 tracker 实例中的 `_model_name`，而不是只打印环境变量。
- 代码格式应修正为规范缩进，避免路径选择逻辑看起来已写但实际没有正确赋值。

### 严格模式规则

- 请求“瓶子”时 GroundingDINO 未加载：失败。
- GroundingDINO 无检测：失败。
- EdgeTAM 未加载或输出空 mask：失败。
- 不能退化成 bbox 矩形仍返回成功。
- 无有效深度：失败。
- 世界坐标超出合理范围：失败。

调试模式可以允许 fallback，但必须：

```text
fallback_used=true
```

并在 CLI 中显示黄色警告。

---

## P7-04：改进“颜色 + 类别”联合识别

当前“绿色物体”走 HSV 是合理的低成本路径，但“绿色瓶子”最好同时利用类别和颜色。

推荐：

1. GroundingDINO 以 `bottle` 检出候选框。
2. 每个候选框计算 HSV 绿色比例。
3. 联合评分：

   ```text
   score = detector_score * color_consistency * depth_plausibility
   ```

4. 选择最高候选。
5. EdgeTAM 对选中框做精细分割。
6. 深度反投影。

保留三种策略：

```text
query=“绿色物体” -> HSV/几何
query=“瓶子”     -> GroundingDINO + EdgeTAM
query=“绿色瓶子” -> GroundingDINO + HSV 重排 + EdgeTAM
```

### 测试

扩展：

- `tests/unit/skills/test_perception_grasp.py`
- `tests/unit/skills/test_grasp_model_routing.py`

覆盖：

- 同时有绿色凳子和绿色瓶子时选择瓶子。
- EdgeTAM 失败时严格模式失败。
- 矩形 fallback 的 diagnostics 为真。
- 不读取 MuJoCo 对象真值。

---

## P7-05：感知结果的几何合理性门控

抓取前检查：

```text
mask_pixels >= min_mask_pixels
valid_depth_points >= min_depth_points
depth_m in [min_depth, max_depth]
target_z 高于地面且接近桌面/物体范围
target 与 base 距离合理
目标在相机视锥内
相邻帧 3D 点一致
```

建议要求 2–3 帧一致：

```text
3D centroid 标准差 < 3 cm
mask IoU > 0.5
```

不满足则重新扫描/对位，不直接送 IK。

---

# P8：机械臂自身碰撞和 Go2 机身防穿模

## P8-01：明确根因

当前 `PickTopDownSkill` 的说明已经明确：

```text
no collision check
```

现有 top-down IK 主要约束：

- 末端位置/姿态。
- 关节上下限。

它没有完整约束：

- Piper 连杆与 Go2 机身碰撞。
- Piper 自碰撞。
- 关节插值扫过路径碰撞。
- 夹爪与桌面碰撞。

即使起点和终点都不碰撞，中间关节插值也可能穿过狗身。

---

## P8-02：先补 MuJoCo 碰撞几何

### 涉及文件

- `vector_os_nano/hardware/sim/mjcf/go2/scene_room_piper.xml`
- `vector_os_nano/hardware/sim/mjcf/go2_piper/go2_piper.xml`
- `vector_os_nano/hardware/sim/mujoco_go2.py`
- Piper 仿真适配类

### 实现步骤

1. 区分 visual geom 和 collision geom。
2. 给 Go2 机身增加简化碰撞盒/胶囊体：

   - 主躯干。
   - 肩部安装区域。
   - 机械臂底座附近保护区。

3. 给 Piper 每根主要连杆增加 capsule/box 碰撞体。
4. 设置合理的 `contype` / `conaffinity`。
5. 排除天然相邻连杆的无意义接触。
6. 不要排除“机械臂 vs Go2 body”接触。
7. 给碰撞体留 1–3 cm 安全膨胀量。
8. 在 viewer 中给调试碰撞体可视化开关。

### 验收

- 手动把臂移动到狗身内部时，MuJoCo 能报告对应 contact。
- 正常 stow 不会持续自碰撞。

---

## P8-03：实现离散轨迹碰撞检查

### 新增接口建议

```text
vector_os_nano/manipulation/collision_checker.py
```

接口：

```python
class ArmCollisionChecker:
    def state_is_safe(self, q: Sequence[float]) -> CollisionResult:
        ...

    def trajectory_is_safe(
        self,
        q_start: Sequence[float],
        q_goal: Sequence[float],
        max_joint_step: float = 0.03,
    ) -> CollisionResult:
        ...
```

MuJoCo 实现：

1. 保存当前 `qpos`。
2. 对轨迹按最大关节步长插值。
3. 每个样本设置 arm qpos。
4. 调用 `mj_forward`。
5. 检查 contact pair。
6. 恢复原 `qpos`。
7. 返回首个碰撞样本、geom 名和最小距离。

注意：

- 碰撞检查不能推进真实仿真时间。
- 必须有 `try/finally` 恢复状态。
- 只过滤已明确允许的接触对。

### 验收

- 起终点安全但中间穿机身的轨迹会被拒绝。
- 日志能指出碰撞的两个 geom。

---

## P8-04：把抓取改为分阶段安全轨迹

### 当前轨迹

```text
当前姿态 -> pre_grasp -> grasp -> close -> lift
```

### 建议轨迹

```text
navigation_stow
  -> shoulder_clear
  -> pre_grasp_high
  -> pre_grasp
  -> vertical_descent
  -> close
  -> vertical_lift
  -> carry_clear
```

每段都必须：

1. IK 有解。
2. 关节限位满足。
3. 起终点状态安全。
4. 插值轨迹安全。
5. 执行中监控意外接触。

### 多 seed IK

对同一末端目标使用多个 seed：

- 当前关节。
- `shoulder_clear`。
- 上一次安全抓取姿态。
- 左/右肘候选。

对每个解评分：

```text
score =
  joint_distance
  + collision_clearance_cost
  + joint_limit_margin_cost
  + body_proximity_cost
```

选择安全且离机身更远的解。

### 验收

- 抓取绿色瓶子的整条轨迹不产生 arm-body contact。
- 无安全解时返回 `collision_unavoidable`，夹爪不盲目闭合。

---

## P8-05：长期方案——MoveIt 2 / OMPL

完成离散检查后，可增加 MoveIt 2：

```text
Piper URDF/SRDF
Go2 body 作为 planning scene collision object
桌面和场景障碍加入 planning scene
OMPL 规划 joint-space trajectory
Time parameterization
MuJoCo 执行与反馈
```

建议顺序：

1. 先完成 P8-02 到 P8-04，快速解决穿模。
2. 再接 MoveIt 2，不要一开始让 MoveIt 同时承担 base 导航。
3. MoveIt 只负责机械臂路径，base 对位仍由 navigation/docking 完成。

---

# P9：未知环境下的边探索、边建图和语义导航

## P9-01：增加显式世界模式

### 配置

```dotenv
VECTOR_WORLD_MODE=known_layout
```

或：

```dotenv
VECTOR_WORLD_MODE=unknown_exploration
```

### known_layout

- 加载 `config/room_layout.yaml`。
- 房间和门标记 `source=layout_prior`。
- 可直接执行 `navigate_room`。

### unknown_exploration

- 不加载房间中心和门真值。
- SceneGraph 初始为空或只含 `unknown_start_area`。
- TARE 负责几何探索。
- 感知/VLM 负责语义命名。
- 只有发现后的房间可以 `navigate_room`。
- 尚未发现时使用 `search_room`。

### 验收

- unknown 模式下查询 kitchen 不会直接得到 `[17.0, 2.5]`。
- known 模式现有仿真仍可快速测试。

---

## P9-02：把探索拆成几何层和语义层

### 目标架构

```text
LiDAR / depth
  -> terrain/elevation map
  -> TARE frontier candidates
  -> frontier utility
       ├── geometric gain
       ├── travel cost
       ├── risk
       └── semantic relevance
  -> FAR/localPlanner 执行
  -> 新观测
       ├── VLM room label
       ├── object detections
       ├── doorway detection
       └── SceneGraph update
```

TARE 保留其几何探索职责，不要求 LLM 直接产生运动轨迹。

---

## P9-03：实现在线 RoomGraph / DoorGraph

### 房间节点

```json
{
  "room_id": "room_003",
  "semantic_label": "kitchen",
  "label_confidence": 0.82,
  "polygon": [],
  "center": [x, y],
  "source": "observed",
  "visit_count": 3,
  "last_seen": 0
}
```

### 门边

```json
{
  "from": "room_002",
  "to": "room_003",
  "center": [x, y],
  "width": 1.1,
  "confidence": 0.76,
  "source": "observed"
}
```

### 实现步骤

1. 基于空间分割/轨迹拓扑生成临时区域节点。
2. 穿过狭窄通道或门检测时创建 DoorEdge。
3. 用 VLM/物体上下文给区域打语义标签。
4. 多帧、多视角投票，不能一次 VLM 输出就永久命名。
5. 标签冲突时保留概率分布。
6. 持久化 SceneGraph。
7. 再次进入已知区域时进行 loop closure/节点合并。

---

## P9-04：实现 `search_room(room)`

### 行为

```text
若 room 已在 SceneGraph：
  直接 navigate_room

若 room 未知：
  选择最有可能发现该房间的 frontier
  -> 导航
  -> 观察和语义分类
  -> 更新 SceneGraph
  -> 若找到目标，结束探索并 navigate_room
  -> 否则继续
```

### 工具结果

```json
{
  "query": "kitchen",
  "status": "FOUND|NOT_FOUND|EXHAUSTED|BLOCKED",
  "explored_frontiers": 7,
  "discovered_rooms": ["hallway", "dining_room", "kitchen"],
  "target_room_id": "room_003"
}
```

### 安全边界

- 设最大探索时间/距离/电量。
- 每个 frontier 必须先通过可达性检查。
- 反复失败的 frontier 加入黑名单。
- 用户 stop 必须立即取消 TARE、FAR 和 bridge。

---

## P9-05：SysNav 风格语义价值接入

项目中 SysNav 相关想法目前不等于已经完成 live 闭环。推荐把它接在“frontier 排序”和“语义地图更新”处，而不是替代底层 FAR/MPC。

可定义：

```text
semantic_score(frontier, query) =
  CLIP/VLM 对当前视野的目标相关性
  + 邻接房间先验
  + 已发现物体上下文
  - 重复观察惩罚
```

例如搜索 kitchen 时：

- 餐桌、冰箱、橱柜相关区域得分更高。
- 已确认卧室的 frontier 得分降低。
- 几何不可达的 frontier 无论语义多高都不能执行。

### 验收

- unknown 模式下目标房间位置不是预置的。
- 系统能输出探索过的 frontier、语义证据和最终房间节点。
- 找到后切换到标准 `navigate_room`，而不是继续探索漂移。

---

# P10：完整测试矩阵和验收

## P10-01：静态和单元测试

每阶段完成后运行对应测试，最终至少运行：

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano

pytest -q \
  tests/unit/test_navigate_skill.py \
  tests/unit/test_navigate_coordinate.py \
  tests/unit/test_navigate_skill_nav2.py \
  tests/unit/vcli/test_native_loop.py \
  tests/vcli/test_native_loop_trichotomy_pty.py

pytest -q \
  tests/harness/test_level24_path_follower.py \
  tests/harness/test_level27_stuck_escape.py \
  tests/harness/test_level29_pre_explore_nav.py \
  tests/harness/test_level30_terrain_persist.py \
  tests/harness/test_level31_wall_escape.py \
  tests/harness/test_level35_hardcode_free_nav.py \
  tests/harness/test_level35b_proxy_door_chain.py \
  tests/harness/test_level36_terrain_replay.py \
  tests/harness/test_level52_doorchain_navstack.py

pytest -q \
  tests/unit/skills/test_perception_grasp.py \
  tests/unit/skills/test_grasp_model_routing.py \
  tests/skills/test_pick_top_down.py \
  tests/hardware/sim/test_mujoco_piper.py
```

如果某个测试文件在当前分支名称略有不同，先用：

```zsh
rg --files tests | rg 'navigate|path_follower|stuck|terrain|grasp|piper|native_loop'
```

不要为了让测试变绿而删除旧断言。`test_native_loop_trichotomy_pty.py` 若固定了“排除正式 NavigateSkill”的旧行为，应把断言更新为新的双工具契约。

---

## P10-02：已知户型导航矩阵

| 编号 | 起点 | 目标 | 必经拓扑 | 验收 |
|---|---|---|---|---|
| N1 | living_room | dining_room | hallway 两侧门 | `in_room(dining_room)` |
| N2 | living_room | kitchen | living→hallway→kitchen | 不穿墙 |
| N3 | kitchen | guest_bedroom | hallway | 正向通过门 |
| N4 | study | master_bedroom | hallway | 无长期倒走 |
| N5 | bathroom | living_room | hallway | 15 秒卡住能失败/重规划 |

每个任务必须检查：

- native 工具是 `navigate_room`。
- canonical room 正确。
- 目标坐标来自 SceneGraph。
- door chain 正确。
- FAR 状态为 `VALID_PATH`。
- 全局路径不与墙相交。
- local path frame 已知。
- 导航主体为正向行走。
- 到达后速度为零。
- `in_room()` 为真。

量化建议：

```text
房间成功率 >= 90%（每条 10 次）
墙体碰撞次数 = 0
错误房间次数 = 0
目标猜坐标次数 = 0
长程负 vx 占比 < 2%
失败后停车延迟 < 0.5 s
```

---

## P10-03：已知户型抓取矩阵

| 编号 | 指令 | 预期感知路由 |
|---|---|---|
| G1 | 抓取前面的绿色物体 | HSV/front-object，可显式显示 |
| G2 | 抓取前面的瓶子 | GroundingDINO + EdgeTAM |
| G3 | 抓取绿色瓶子 | GroundingDINO + HSV 重排 + EdgeTAM |
| G4 | 抓取前面的东西 | depth/geometry front-object |
| G5 | 导航到桌前并抓取绿色瓶子 | 导航→对位→重感知→抓取 |

验收：

- strict 模式下 G2/G3 没有模型时必须失败。
- diagnostics 与实际路由一致。
- 3D 点来自 RGB-D，不来自仿真对象真值。
- IK 全轨迹碰撞检查通过。
- arm-body contact 为 0。
- 抓取后 `holding_object(canonical_id)` 为真。

---

## P10-04：未知环境测试

测试前：

- `VECTOR_WORLD_MODE=unknown_exploration`
- 使用新的 SceneGraph 文件。
- 不加载 `room_layout.yaml` 房间中心。

任务：

```text
“找到 kitchen 并导航进去”
```

验收：

1. 初始 SceneGraph 不含 kitchen 中心。
2. CLI 调用 `search_room("kitchen")`，不是猜 `navigate_xy`。
3. TARE 产生 frontier。
4. FAR/localPlanner 执行几何移动。
5. VLM/语义模块给出可追溯证据。
6. 在线建立 RoomGraph/DoorGraph。
7. 发现 kitchen 后停止探索。
8. 再调用 `navigate_room` 进入目标。
9. 最终 `in_room(kitchen)` 通过。

---

## P10-05：完整狗臂 demo 流程

### 环境变量建议

`.env` 仅放密钥和项目级配置，不把密钥写入本文档或 Git。

```dotenv
VECTOR_WORLD_MODE=known_layout
VECTOR_GRASP_STRICT_MODE=1
VECTOR_REQUIRE_GROUNDING_DINO=1
VECTOR_REQUIRE_EDGETAM=1
VECTOR_ALLOW_BOX_MASK_FALLBACK=0
VECTOR_ALLOW_SIM_GROUND_TRUTH=0

EDGETAM_MODEL_PATH=/media/fishyu/fish-14tb-2/YuXi/go2armagent/models/EdgeTAM-hf

VECTOR_NAV_ALLOW_REVERSE=0
VECTOR_NAV_STALL_TIMEOUT_S=15
VECTOR_NAV_MAX_ESCAPE_COUNT=2
VECTOR_NAV_MAX_VX=0.30
VECTOR_NAV_MAX_WZ=0.55
```

实际变量名应在实现时集中由一个配置模块读取，避免脚本、Python 和 `.env` 各自使用不同名称。

### 启动前检查

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano

python - <<'PY'
import os
from pathlib import Path
import torch

p = Path(os.environ["EDGETAM_MODEL_PATH"]).expanduser()
print("CUDA:", torch.cuda.is_available())
print("GPU:", torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)
print("EdgeTAM:", p)
print("config.json:", (p / "config.json").exists())
PY

ros2 pkg prefix local_planner
ros2 pkg prefix far_planner
ros2 pkg prefix tare_planner
```

### 启动

终端 A：

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano
./scripts/launch_explore.sh
```

终端 B：

```zsh
source /media/fishyu/fish-14tb-2/YuXi/go2armagent/activate_go2arm.sh
cd /media/fishyu/fish-14tb-2/YuXi/go2armagent/vector-os-nano
vector-cli
```

### CLI 正式测试序列

```text
1. 打开带 Piper 机械臂的 Go2 MuJoCo 仿真
2. 导航到 dining room
3. 找到绿色瓶子
4. 调整底座使相机正对绿色瓶子
5. 抓取绿色瓶子
6. 导航到 living room
7. 验证仍持有绿色瓶子
```

预期工具链：

```text
start_simulation(sim_type=go2, with_arm=true, gui=true)
  -> navigate_room(room="dining room")
  -> in_room("dining_room")
  -> detect/scan target
  -> terminal_dock(...)
  -> perception_grasp(query="绿色瓶子")
  -> holding_object("pickable_bottle_green")
  -> navigate_room(room="living room")
  -> in_room("living_room")
  -> holding_object("pickable_bottle_green")
```

---

# 3. 配置集中化建议

当前参数散落在 shell、YAML、环境变量和 Python 常量中。建议新增：

```text
config/go2arm_demo.yaml
```

示意：

```yaml
world:
  mode: known_layout
  room_layout: config/room_layout.yaml

navigation:
  robot_config: unitree/unitree_go2
  allow_reverse_long_range: false
  max_vx: 0.30
  max_vy: 0.12
  max_wz: 0.55
  stall_timeout_s: 15
  max_escape_count: 2
  require_far_valid_path: true
  reject_unknown_path_frame: true

exploration:
  enabled: true
  autostart: false
  fresh_map: false

perception:
  strict: true
  grounding_dino:
    required: true
  edgetam:
    required: true
    model_path_env: EDGETAM_MODEL_PATH
  allow_box_fallback: false
  allow_sim_ground_truth: false

manipulation:
  collision_check: true
  collision_sample_step_rad: 0.03
  safe_clearance_m: 0.02

locomotion:
  controller: convex_mpc
  robot_model: go2_piper
  navigation_stow_required: true
```

优先级：

```text
CLI 参数 > 环境变量 > go2arm_demo.yaml > 安全默认值
```

启动时打印最终生效配置，并保存到 run artifact。

---

# 4. 推荐的代码目录调整

不要求一次性重构，可随阶段逐步迁移：

```text
vector_os_nano/
  navigation/
    room_resolver.py
    goal.py
    waypoint_mux.py
    progress_monitor.py
    path_frames.py
    door_route.py
  exploration/
    mode.py
    semantic_frontier.py
    room_graph_builder.py
  manipulation/
    collision_checker.py
    staged_grasp.py
    arm_base_interlock.py
  diagnostics/
    run_context.py
    navigation_trace.py
    grasp_trace.py
```

保留现有 public skill 入口，通过内部调用新模块逐步迁移，避免一次性改坏所有测试。

---

# 5. 每阶段的提交和回滚策略

每一阶段建议：

```zsh
git status --short
git diff --stat
git diff --check
pytest -q <本阶段测试>
```

提交示例：

```text
fix(nav): expose deterministic room navigation in native CLI
fix(nav): route known rooms through scene graph door chain
fix(nav): separate FAR and TARE waypoint ownership
fix(nav): make stall recovery bounded and forward-first
fix(map): merge persisted terrain before atomic save
feat(perception): report detector and segmenter diagnostics
fix(arm): reject colliding Piper trajectories
feat(explore): add unknown semantic room search mode
```

每个提交都应满足：

- 只包含一个主题。
- 有对应测试。
- 有前后日志或截图。
- 有明确配置变更。
- 能单独回滚，不依赖删除用户原有代码。

---

# 6. Definition of Done

## 命名房间导航

- [ ] native CLI 暴露 `navigate_room` 和 `navigate_xy`。
- [ ] 命名房间不再由 LLM 猜坐标。
- [ ] `dining room` 解析为 `dining_room`，目标来自 SceneGraph。
- [ ] `in_room(room_id)` 可确定性验收。
- [ ] 导航 actor causation 能识别 bridge 执行的速度命令。

## 路径规划

- [ ] 已知房间跨房导航先计算门链。
- [ ] FAR 显式报告 `VALID_PATH`/`NO_PATH`。
- [ ] FAR、TARE waypoint 来源分离。
- [ ] RViz 同时显示门链、全局路径、局部路径和实际轨迹。
- [ ] 所有 Path frame 都被验证或转换。
- [ ] 没有路径时立即停车。

## 运动控制

- [ ] 保留 `robot_config:=unitree/unitree_go2`。
- [ ] 长程 `twoWayDrive=false`。
- [ ] 目标在身后时先原地转向。
- [ ] DOCK/ESCAPE 之外不倒车。
- [ ] 卡住恢复次数有上限。
- [ ] 累计无进展超时不会被 escape 重置。
- [ ] Convex MPC 输入、输出和跟踪误差可记录。
- [ ] 导航时机械臂进入稳定 stow。
- [ ] MPC 参数考虑 Piper 等效载荷。

## 地图

- [ ] 旧地图合并进入 accumulator。
- [ ] 保存为 old ∪ new，而非仅本轮局部点。
- [ ] 原子写入并保留历史版本。
- [ ] 地面和障碍不再只用单一最大 Z 表示。
- [ ] 普通导航不会取消地图 replay。

## 感知抓取

- [ ] CLI 显示 resolver、detector、segmenter 和 fallback。
- [ ] “绿色物体”是否走 HSV 清晰可见。
- [ ] “瓶子”可验证走 GroundingDINO + EdgeTAM。
- [ ] EdgeTAM 直接读取 `EDGETAM_MODEL_PATH`，无需复制模型。
- [ ] strict 模式禁止静默 bbox fallback。
- [ ] 3D 抓取点只来自实际 RGB-D/掩码。
- [ ] 多帧几何一致性通过后才执行 IK。

## 机械臂安全

- [ ] Go2 body 和 Piper link 有有效碰撞 geom。
- [ ] 轨迹全程离散碰撞检查。
- [ ] 采用 shoulder-clear/pre-grasp/descent/lift 分阶段路径。
- [ ] 无安全解时明确失败。
- [ ] 抓取过程中 arm-body contact 为 0。

## 未知环境探索

- [ ] known/unknown 两种 world mode 明确分离。
- [ ] unknown 模式不读取预设房间中心。
- [ ] TARE 负责几何 frontier。
- [ ] VLM/SysNav 层负责语义 frontier 排序和房间标注。
- [ ] 在线更新 RoomGraph/DoorGraph。
- [ ] `search_room` 找到房间后切换标准导航。

## 完整 demo

- [ ] 从 living_room 到 dining_room 不穿墙。
- [ ] 狗头朝向行进方向。
- [ ] 到桌前后完成 terminal dock。
- [ ] GroundingDINO/EdgeTAM 路由可验证。
- [ ] Piper 抓取不穿 Go2 机身。
- [ ] 持物导航到下一房间仍保持稳定。
- [ ] 全流程日志、路径、感知结果可按 run_id 回放。

---

# 7. 最先建议实际动手的五项

如果要从这份清单开始逐步修，建议严格按以下顺序：

1. **P1-02：拆分 `navigate_room` / `navigate_xy`**  
   先消除 LLM 猜坐标，这是“导航到错误点”的直接根因。

2. **P2-03：命名房间强制走 SceneGraph 门链**  
   先保证跨房间目标不会直接连线穿墙。

3. **P3-01 + P3-02：分离 waypoint 来源、增加 FAR 有效路径状态**  
   让系统知道当前究竟由谁在规划，以及是否真的有路。

4. **P4-01 + P4-02：正向优先和有界卡住恢复**  
   解决倒着走、顶墙、无限 escape。

5. **P7-02 + P8-03：感知 diagnostics 和整条机械臂轨迹碰撞检查**  
   分别解决“到底有没有调用模型”和“机械臂穿狗身”。

完成这五项后，再做地图持久化、MPC 载荷和未知环境 SysNav 语义探索，风险最低，也最容易逐步验证。
